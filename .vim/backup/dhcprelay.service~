[Unit]
Description=Forward DHCP packages from @IFNAME_ETHEXT@ to ethhost (and vice-versa)
After=network-online.target
Requires=network-online.target

[Service]
ExecStart=/usr/sbin/dhcprelay @IFNAME_ETHEXT@ ethhost 172.128.2.20

[Install]
WantedBy=multi-user.target
