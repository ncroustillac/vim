[Unit]
Description=Hostapd client that handle authentication notification
After=hostapd.service

[Service]
Type=forking
PIDFile=/run/hostapd.pid
ExecStartPost=@SBINDIR@/hostapd_cli -p /var/hostapd -a /usr/services/hostapd/hostapd-hooks -B

[Install]
WantedBy=multi-user.target

