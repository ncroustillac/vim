--- git/arch/arm/boot/dts/am57xx-pcm-948-common.dtsi	2023-03-16 16:20:28.590123276 +0100
+++ git/arch/arm/boot/dts/am57xx-pcm-948-common_patched.dtsi	2023-03-16 16:25:02.890902749 +0100
@@ -671,13 +671,21 @@
 	pinctrl-0 = <&dcan2_pins>;
 };
 
-
 &mcspi1 {
-	spidev1_0: spidev1@0 {
-		compatible = "rohm,dh2228fv";
-		
+	/* fixed 8MHz oscillator */
+	sc16clk: oscillator {
+		#clock-cells = <0>;
+		compatible = "fixed-clock";
+		clock-frequency = <8000000>;
+	};
+
+	sc16is760: sc16is760@0 {
+		compatible = "nxp,sc16is760";
 		reg = <0>;
-		spi-max-frequency = <48000000>;
+		clocks = <&sc16clk>;
+		interrupt-parent = <&gpio5>;
+		interrupts = <7 IRQ_TYPE_EDGE_FALLING>;
+		spi-max-frequency = <12000000>;
 	};
 };
 
